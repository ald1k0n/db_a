import React from 'react';
import {Command} from "Features";

export const MailingPage = () => {
    return (
        <section className={'container mx-auto py-10 grow flex flex-col gap-4'}>
            <Command />
        </section>
    );
};